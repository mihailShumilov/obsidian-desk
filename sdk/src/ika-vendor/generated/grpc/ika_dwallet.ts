// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.21.12
// source: ika_dwallet.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "ika.dwallet.v1";

/**
 * All mutation requests are wrapped in UserSignedRequest.
 * The UserSignature is self-contained: it carries both the signature bytes
 * and the public key bytes, with the variant determining the scheme
 * (Ed25519, Secp256k1, Secp256r1). Every validator independently verifies.
 */
export interface UserSignedRequest {
  /**
   * BCS-serialized UserSignature enum (carries signature + public key + scheme).
   * Variants: Ed25519 { signature, public_key }, Secp256k1 { ... }, Secp256r1 { ... }
   */
  userSignature: Uint8Array;
  /**
   * BCS-serialized SignedRequestData (the signed payload).
   * Contains session_identifier_preimage, epoch, chain_id, intended_chain_sender,
   * and the DWalletRequest enum variant that determines the operation.
   */
  signedRequestData: Uint8Array;
}

/** Response for SubmitTransaction -- wraps BCS-serialized TransactionResponseData. */
export interface TransactionResponse {
  /** BCS-serialized TransactionResponseData enum (Signature, Attestation, Presign, or Error). */
  responseData: Uint8Array;
}

export interface GetPresignsRequest {
  userPubkey: Uint8Array;
}

export interface GetPresignsForDWalletRequest {
  userPubkey: Uint8Array;
  dwalletId: Uint8Array;
}

export interface GetPresignsResponse {
  presigns: PresignInfo[];
}

export interface PresignInfo {
  presignId: Uint8Array;
  dwalletId: Uint8Array;
  curve: number;
  signatureScheme: number;
  epoch: number;
}

function createBaseUserSignedRequest(): UserSignedRequest {
  return { userSignature: new Uint8Array(0), signedRequestData: new Uint8Array(0) };
}

export const UserSignedRequest: MessageFns<UserSignedRequest> = {
  encode(message: UserSignedRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userSignature.length !== 0) {
      writer.uint32(10).bytes(message.userSignature);
    }
    if (message.signedRequestData.length !== 0) {
      writer.uint32(18).bytes(message.signedRequestData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserSignedRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserSignedRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userSignature = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.signedRequestData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserSignedRequest {
    return {
      userSignature: isSet(object.userSignature)
        ? bytesFromBase64(object.userSignature)
        : isSet(object.user_signature)
        ? bytesFromBase64(object.user_signature)
        : new Uint8Array(0),
      signedRequestData: isSet(object.signedRequestData)
        ? bytesFromBase64(object.signedRequestData)
        : isSet(object.signed_request_data)
        ? bytesFromBase64(object.signed_request_data)
        : new Uint8Array(0),
    };
  },

  toJSON(message: UserSignedRequest): unknown {
    const obj: any = {};
    if (message.userSignature.length !== 0) {
      obj.userSignature = base64FromBytes(message.userSignature);
    }
    if (message.signedRequestData.length !== 0) {
      obj.signedRequestData = base64FromBytes(message.signedRequestData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserSignedRequest>, I>>(base?: I): UserSignedRequest {
    return UserSignedRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserSignedRequest>, I>>(object: I): UserSignedRequest {
    const message = createBaseUserSignedRequest();
    message.userSignature = object.userSignature ?? new Uint8Array(0);
    message.signedRequestData = object.signedRequestData ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTransactionResponse(): TransactionResponse {
  return { responseData: new Uint8Array(0) };
}

export const TransactionResponse: MessageFns<TransactionResponse> = {
  encode(message: TransactionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.responseData.length !== 0) {
      writer.uint32(10).bytes(message.responseData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TransactionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTransactionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responseData = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TransactionResponse {
    return {
      responseData: isSet(object.responseData)
        ? bytesFromBase64(object.responseData)
        : isSet(object.response_data)
        ? bytesFromBase64(object.response_data)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TransactionResponse): unknown {
    const obj: any = {};
    if (message.responseData.length !== 0) {
      obj.responseData = base64FromBytes(message.responseData);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TransactionResponse>, I>>(base?: I): TransactionResponse {
    return TransactionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TransactionResponse>, I>>(object: I): TransactionResponse {
    const message = createBaseTransactionResponse();
    message.responseData = object.responseData ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetPresignsRequest(): GetPresignsRequest {
  return { userPubkey: new Uint8Array(0) };
}

export const GetPresignsRequest: MessageFns<GetPresignsRequest> = {
  encode(message: GetPresignsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userPubkey.length !== 0) {
      writer.uint32(10).bytes(message.userPubkey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPresignsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPresignsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userPubkey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPresignsRequest {
    return {
      userPubkey: isSet(object.userPubkey)
        ? bytesFromBase64(object.userPubkey)
        : isSet(object.user_pubkey)
        ? bytesFromBase64(object.user_pubkey)
        : new Uint8Array(0),
    };
  },

  toJSON(message: GetPresignsRequest): unknown {
    const obj: any = {};
    if (message.userPubkey.length !== 0) {
      obj.userPubkey = base64FromBytes(message.userPubkey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPresignsRequest>, I>>(base?: I): GetPresignsRequest {
    return GetPresignsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPresignsRequest>, I>>(object: I): GetPresignsRequest {
    const message = createBaseGetPresignsRequest();
    message.userPubkey = object.userPubkey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetPresignsForDWalletRequest(): GetPresignsForDWalletRequest {
  return { userPubkey: new Uint8Array(0), dwalletId: new Uint8Array(0) };
}

export const GetPresignsForDWalletRequest: MessageFns<GetPresignsForDWalletRequest> = {
  encode(message: GetPresignsForDWalletRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userPubkey.length !== 0) {
      writer.uint32(10).bytes(message.userPubkey);
    }
    if (message.dwalletId.length !== 0) {
      writer.uint32(18).bytes(message.dwalletId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPresignsForDWalletRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPresignsForDWalletRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userPubkey = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dwalletId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPresignsForDWalletRequest {
    return {
      userPubkey: isSet(object.userPubkey)
        ? bytesFromBase64(object.userPubkey)
        : isSet(object.user_pubkey)
        ? bytesFromBase64(object.user_pubkey)
        : new Uint8Array(0),
      dwalletId: isSet(object.dwalletId)
        ? bytesFromBase64(object.dwalletId)
        : isSet(object.dwallet_id)
        ? bytesFromBase64(object.dwallet_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: GetPresignsForDWalletRequest): unknown {
    const obj: any = {};
    if (message.userPubkey.length !== 0) {
      obj.userPubkey = base64FromBytes(message.userPubkey);
    }
    if (message.dwalletId.length !== 0) {
      obj.dwalletId = base64FromBytes(message.dwalletId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPresignsForDWalletRequest>, I>>(base?: I): GetPresignsForDWalletRequest {
    return GetPresignsForDWalletRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPresignsForDWalletRequest>, I>>(object: I): GetPresignsForDWalletRequest {
    const message = createBaseGetPresignsForDWalletRequest();
    message.userPubkey = object.userPubkey ?? new Uint8Array(0);
    message.dwalletId = object.dwalletId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGetPresignsResponse(): GetPresignsResponse {
  return { presigns: [] };
}

export const GetPresignsResponse: MessageFns<GetPresignsResponse> = {
  encode(message: GetPresignsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.presigns) {
      PresignInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPresignsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPresignsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.presigns.push(PresignInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPresignsResponse {
    return {
      presigns: globalThis.Array.isArray(object?.presigns)
        ? object.presigns.map((e: any) => PresignInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPresignsResponse): unknown {
    const obj: any = {};
    if (message.presigns?.length) {
      obj.presigns = message.presigns.map((e) => PresignInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPresignsResponse>, I>>(base?: I): GetPresignsResponse {
    return GetPresignsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPresignsResponse>, I>>(object: I): GetPresignsResponse {
    const message = createBaseGetPresignsResponse();
    message.presigns = object.presigns?.map((e) => PresignInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBasePresignInfo(): PresignInfo {
  return { presignId: new Uint8Array(0), dwalletId: new Uint8Array(0), curve: 0, signatureScheme: 0, epoch: 0 };
}

export const PresignInfo: MessageFns<PresignInfo> = {
  encode(message: PresignInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.presignId.length !== 0) {
      writer.uint32(10).bytes(message.presignId);
    }
    if (message.dwalletId.length !== 0) {
      writer.uint32(18).bytes(message.dwalletId);
    }
    if (message.curve !== 0) {
      writer.uint32(24).uint32(message.curve);
    }
    if (message.signatureScheme !== 0) {
      writer.uint32(32).uint32(message.signatureScheme);
    }
    if (message.epoch !== 0) {
      writer.uint32(40).uint64(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PresignInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePresignInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.presignId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dwalletId = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.curve = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.signatureScheme = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.epoch = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PresignInfo {
    return {
      presignId: isSet(object.presignId)
        ? bytesFromBase64(object.presignId)
        : isSet(object.presign_id)
        ? bytesFromBase64(object.presign_id)
        : new Uint8Array(0),
      dwalletId: isSet(object.dwalletId)
        ? bytesFromBase64(object.dwalletId)
        : isSet(object.dwallet_id)
        ? bytesFromBase64(object.dwallet_id)
        : new Uint8Array(0),
      curve: isSet(object.curve) ? globalThis.Number(object.curve) : 0,
      signatureScheme: isSet(object.signatureScheme)
        ? globalThis.Number(object.signatureScheme)
        : isSet(object.signature_scheme)
        ? globalThis.Number(object.signature_scheme)
        : 0,
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
    };
  },

  toJSON(message: PresignInfo): unknown {
    const obj: any = {};
    if (message.presignId.length !== 0) {
      obj.presignId = base64FromBytes(message.presignId);
    }
    if (message.dwalletId.length !== 0) {
      obj.dwalletId = base64FromBytes(message.dwalletId);
    }
    if (message.curve !== 0) {
      obj.curve = Math.round(message.curve);
    }
    if (message.signatureScheme !== 0) {
      obj.signatureScheme = Math.round(message.signatureScheme);
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PresignInfo>, I>>(base?: I): PresignInfo {
    return PresignInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PresignInfo>, I>>(object: I): PresignInfo {
    const message = createBasePresignInfo();
    message.presignId = object.presignId ?? new Uint8Array(0);
    message.dwalletId = object.dwalletId ?? new Uint8Array(0);
    message.curve = object.curve ?? 0;
    message.signatureScheme = object.signatureScheme ?? 0;
    message.epoch = object.epoch ?? 0;
    return message;
  },
};

export type DWalletServiceService = typeof DWalletServiceService;
export const DWalletServiceService = {
  /**
   * Submit any dWallet operation (DKG, sign, presign, etc.).
   * The request type is encoded in the BCS-serialized SignedRequestData.request
   * (DWalletRequest enum variant), which is covered by the user's signature.
   */
  submitTransaction: {
    path: "/ika.dwallet.v1.DWalletService/SubmitTransaction" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UserSignedRequest): Buffer => Buffer.from(UserSignedRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UserSignedRequest => UserSignedRequest.decode(value),
    responseSerialize: (value: TransactionResponse): Buffer => Buffer.from(TransactionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): TransactionResponse => TransactionResponse.decode(value),
  },
  /** Get all global presigns for a user. */
  getPresigns: {
    path: "/ika.dwallet.v1.DWalletService/GetPresigns" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetPresignsRequest): Buffer => Buffer.from(GetPresignsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetPresignsRequest => GetPresignsRequest.decode(value),
    responseSerialize: (value: GetPresignsResponse): Buffer => Buffer.from(GetPresignsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetPresignsResponse => GetPresignsResponse.decode(value),
  },
  /** Get all dWallet-specific presigns for a user. */
  getPresignsForDWallet: {
    path: "/ika.dwallet.v1.DWalletService/GetPresignsForDWallet" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetPresignsForDWalletRequest): Buffer =>
      Buffer.from(GetPresignsForDWalletRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetPresignsForDWalletRequest => GetPresignsForDWalletRequest.decode(value),
    responseSerialize: (value: GetPresignsResponse): Buffer => Buffer.from(GetPresignsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetPresignsResponse => GetPresignsResponse.decode(value),
  },
} as const;

export interface DWalletServiceServer extends UntypedServiceImplementation {
  /**
   * Submit any dWallet operation (DKG, sign, presign, etc.).
   * The request type is encoded in the BCS-serialized SignedRequestData.request
   * (DWalletRequest enum variant), which is covered by the user's signature.
   */
  submitTransaction: handleUnaryCall<UserSignedRequest, TransactionResponse>;
  /** Get all global presigns for a user. */
  getPresigns: handleUnaryCall<GetPresignsRequest, GetPresignsResponse>;
  /** Get all dWallet-specific presigns for a user. */
  getPresignsForDWallet: handleUnaryCall<GetPresignsForDWalletRequest, GetPresignsResponse>;
}

export interface DWalletServiceClient extends Client {
  /**
   * Submit any dWallet operation (DKG, sign, presign, etc.).
   * The request type is encoded in the BCS-serialized SignedRequestData.request
   * (DWalletRequest enum variant), which is covered by the user's signature.
   */
  submitTransaction(
    request: UserSignedRequest,
    callback: (error: ServiceError | null, response: TransactionResponse) => void,
  ): ClientUnaryCall;
  submitTransaction(
    request: UserSignedRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: TransactionResponse) => void,
  ): ClientUnaryCall;
  submitTransaction(
    request: UserSignedRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: TransactionResponse) => void,
  ): ClientUnaryCall;
  /** Get all global presigns for a user. */
  getPresigns(
    request: GetPresignsRequest,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
  getPresigns(
    request: GetPresignsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
  getPresigns(
    request: GetPresignsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
  /** Get all dWallet-specific presigns for a user. */
  getPresignsForDWallet(
    request: GetPresignsForDWalletRequest,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
  getPresignsForDWallet(
    request: GetPresignsForDWalletRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
  getPresignsForDWallet(
    request: GetPresignsForDWalletRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetPresignsResponse) => void,
  ): ClientUnaryCall;
}

export const DWalletServiceClient = makeGenericClientConstructor(
  DWalletServiceService,
  "ika.dwallet.v1.DWalletService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): DWalletServiceClient;
  service: typeof DWalletServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
