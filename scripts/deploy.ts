/**
 * scripts/deploy.ts — placeholder.
 *
 * Real deployment helper lands in P9 (devnet program publish + IDL upload).
 * Until then this just prints intent so the workspace has a stable script entry.
 */

console.log('[deploy] placeholder — implemented in P9');
