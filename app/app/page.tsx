export default function Page() {
  return (
    <main className="flex min-h-screen items-center justify-center p-12">
      <div className="space-y-4 text-center">
        <h1 className="text-4xl font-semibold tracking-tight">ObsidianDesk</h1>
        <p className="text-muted">
          The dark pool where Bitcoin never leaves Bitcoin.
        </p>
        <p className="text-sm text-muted">P1 scaffold — UI lands in P5.</p>
      </div>
    </main>
  );
}
